export default class query{
    constructor(name, email, questions, answers)
    {
        this.name = name;
        this.email = email;
        this.questions = questions;
        this.answers = answers;
    }
}